function numerator(a){
  let c = Math.random(a);
  return c;
}

function num(a) {
  let  d = numerator(a)+ 0; 
  return d;
}


//chemistry formula of the day grade 8
for (let element=0; element<=4; element++) {
	// think why starts at 83
	for ( let degree=1; degree<=10;degree++) { 
    	console.log("answer:", numerator(2)/num(2)+1);
		}	
    }

